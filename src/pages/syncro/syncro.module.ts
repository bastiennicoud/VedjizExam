import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SyncroPage } from './syncro';

@NgModule({
  declarations: [
    SyncroPage,
  ],
  imports: [
    IonicPageModule.forChild(SyncroPage),
  ],
})
export class SyncroPageModule {}
